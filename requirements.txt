Flask==3.0.0
numpy==2.3.5
Werkzeug==3.0.1
scipy==1.11.4

# Note: GGWave encoding/decoding requires gyges_encoder.exe and gyges_decoder.exe binaries
# Place them in bin/Release/, bin/Debug/, or bin/MinSizeRel/
# If binaries are not found, the code will use a simple FSK fallback (always available)

# Audio playback (install one):
# Option 1 (Recommended): pip install pyaudio
# Option 2: pip install sounddevice

