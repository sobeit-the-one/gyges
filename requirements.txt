Flask==3.0.0
numpy==2.3.5
Werkzeug==3.0.1
scipy==1.11.4

# Audio encoding backends (try installing in order of preference):
# Option 1 (Recommended): pip install ggwave-wheels
# Option 2: pip install ggwave-python
# If none work, the code will use a simple FSK fallback

# Audio playback (install one):
# Option 1 (Recommended): pip install pyaudio
# Option 2: pip install sounddevice

